overwrite: true
schema: "http://localhost:8000/__graphql"
documents:
  - ./src/**/*.{ts,tsx}
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
  ./graphql.schema.json:
    plugins:
      - "introspection"
